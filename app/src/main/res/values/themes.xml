<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ExoBoost" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/theme_dark_background</item>
        <item name="android:navigationBarColor">@color/theme_dark_background</item>
    </style>

    <!-- Transparent Activity Theme for Permission Prompts and Capture -->
    <style name="Theme.ExoBoost.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
